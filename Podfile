# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'MyLife2' do
  use_frameworks!

  target 'MyLife2Tests' do
    inherit! :search_paths
    pod 'Quick', '~> 1.0'
    pod 'Nimble', '~> 5.1'
  end
end
